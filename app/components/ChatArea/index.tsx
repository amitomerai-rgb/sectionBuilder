const ChatArea = () => {
  return (
    <div
      style={{
        minWidth: "300px",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      chat
    </div>
  );
};

export default ChatArea;
